# Focus Sound

ブラウザ完結のアンビエントサウンド集中アプリです。`index.html` 単体で動作し、外部サーバー、ビルドツール、依存パッケージは不要です。

## 実装内容

- Web Audio API でホワイト、ピンク、ブラウンノイズを生成
- 合成環境音として雨、風、波、焚き火を生成
- 複数サウンドの同時再生、個別音量、マスターボリューム
- フェードイン、フェードアウト
- タイマー、自動停止
- プリセットとカスタムプリセット保存
- localStorage に音量、タイマー、プリセットを保存
- Canvas ビジュアライザー
- 外部音源URL、ラジオURL、ローカル音声ファイルの追加
- URL未入力時は Radio Browser API からテンプレートに合うラジオ候補を検索
- 外部音源欄からカフェ、鳥の声、人混み、Jazz のラジオ候補を追加
- 人混みは Wikimedia Commons の Public Domain 音源 `Restaurant ambience.ogg` に固定
- カフェはBossa Nova系の候補に限定し、Bossa Cafeとして追加
- 外部音源はテンプレート由来を保存し、再生中は外部音源ボタンにもアクティブ表示

## 起動

`index.html` をブラウザで開くだけで動作します。ネットワークアクセスは不要です。
